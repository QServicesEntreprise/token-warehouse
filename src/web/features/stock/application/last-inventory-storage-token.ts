import { InjectionToken } from '@angular/core';
import { LastInventoryStorage } from './last-inventory-storage';

export const LAST_INVENTORY_STORAGE = new InjectionToken<LastInventoryStorage>('LAST_INVENTORY_STORAGE');
